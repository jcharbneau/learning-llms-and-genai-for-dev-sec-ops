# TASK CONTRACT

GOAL
- Add a health endpoint that returns `{ "ok": true }`
- Add a minimal test for it

CONSTRAINTS
- Use existing repo tools only (no new deps)
- Keep changes under 3 files

FORMAT
- Plan + Contract Check first
- Then diffs

FAILURE CONDITIONS
- Reject if you add deps
- Reject if you touch `.env` or scan excluded dirs
